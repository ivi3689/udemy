//para ver los errores con javascript
try {
    var year = 2019;
    alert(year);
    console.log(decodeURIComponent("https://victorroblesweb.es"));
    // se usa cuando hay errores en la url la array o las variables
} catch (error) {
    console.log(error);
    alert('Ha ocurrido un error en el codigo');
}
